<div class='container fluid buttons'>

<br>

  <p id="notice"><%= notice %></p>

  <h1>Your Templates</h1>

<h5 class='well'>
  These are Templates that have been created in your Rails application. They create the data for your database tables and form your API calls that can be found in the Template Model.

  <br><br>

  Click "view" in order to create the Template on HelloSign.
</h5>

  <table class='table'>
    <thead>
      <tr>
        <th>Title</th>
        <th>Subject</th>
        <th>Message</th>
        <th>File</th>
      </tr>
    </thead>

    <tbody>
      <% @templates.each do |template| %>
        <tr>
          <td><%= template.title %></td>
          <td><%= template.subject %></td>
          <td><%= template.message %></td>
          <td><%= template.file %></td>
          <td><%= link_to 'View', template %></td>
          <td><%= link_to 'Delete', template, method: :delete, data: { confirm: 'Are you sure?' } %></td>
          <td><%= link_to 'View Signature Requests', template_signature_requests_path(template), class: 'btn btn-md btn-primary' %></td>
        </tr>
      <% end %>
    </tbody>
  </table>

    <%= link_to 'New Template', new_template_path, class: 'btn btn-md btn-primary new_template col-md-12 col-md-offset-0' %>

</div>
